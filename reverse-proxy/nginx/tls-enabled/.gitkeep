# 활성 HTTPS(:443) server 블록을 담는다(런타임 상태 — dev-routes 와 동일 패턴).
# 【현재 비어 있음 = HTTPS 비활성】. tls-available/<...>.conf 를 여기로 복사하면 :443 이 켜진다.
# 실제 *.conf 는 .gitignore 로 제외(공유 브랜치·main 오염 방지) — 이 .gitkeep 만 추적해 빈 디렉터리 보존.
